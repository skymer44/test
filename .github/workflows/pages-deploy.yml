# 🚫 GitHub Pages DÉSACTIVÉ - Site déployé sur Netlify
# 
# Ce workflow est désactivé car le site est maintenant déployé sur Netlify.
# URL de production : https://fichemusicien.site
#
# Pour réactiver GitHub Pages, supprimez les commentaires ci-dessous :

# name: 🌐 Deploy to GitHub Pages
# 
# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:
# 
# permissions:
#   contents: read
#   pages: write
#   id-token: write
# 
# concurrency:
#   group: "pages"
#   cancel-in-progress: false
# 
# jobs:
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: 🔄 Checkout
#         uses: actions/checkout@v4
#         
#       - name: 🔧 Setup Pages
#         uses: actions/configure-pages@v4
#         
#       - name: 📦 Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: '.'
#           
#       - name: 🚀 Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
